#include <iostream>
#include <string>
using namespace std;
int main(){
    string s;
    cin>>s;
    char st[100];
    int top=-1;
    for(int i=0;i<s.size();i++) st[++top]=s[i];
    while(top>=0) cout<<st[top--];
}
